/**
 * Démonstration des 18 fonctions Object 2.
 * Reproduit les cas d'usage de l'énoncé.
 */

const section = (n: number, title: string): void => {
  console.log(`\n=== ${n}. ${title} ===`);
};

// Les imports et appels seront ajoutés fonction par fonction.
void section;
